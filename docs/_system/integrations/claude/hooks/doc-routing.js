// Generated by OttoDoc. Do not edit - the next converge overwrites this file.
//
// Injects OttoDoc's two standing obligations - routing from the tree, and
// settling documentation before a change lands - into every user prompt via the
// Claude Code UserPromptSubmit hook. The static CLAUDE.md block states the same
// obligations, but static context loses to task momentum on judgment tasks;
// context injected at prompt time does not. This script complements the block,
// it does not replace it.
'use strict';

const context =
  'Documentation routing (from the docs/ knowledge tree): Before forming any ' +
  'plan, evaluation, or recommendation - including judgments about ' +
  'issue-tracker items - open docs/index.md, descend the kind indexes whose ' +
  'reader-question bears on this task, and read the bearing documents. Code ' +
  'and tracker items answer only what exists; the tree answers what is ' +
  'intended, constrained, or already decided. A verdict reached before ' +
  'consulting the bearing documents is a guess, however well the code was ' +
  'read. Coverage is part of correctness: each index describes its documents ' +
  'in one sentence precisely so you can decide relevance without opening ' +
  'them - walk every kind index, list every document whose sentence bears on ' +
  'any part of the task, and read each one on that list before concluding. ' +
  'The common failure is reading two documents and stopping; the decisions ' +
  'index in particular tends to bear on far more of a backlog or evaluation ' +
  'task than it first appears. If docs/reference/glossary.md exists, it is ' +
  'the project\'s domain vocabulary: speak in its canonical terms, challenge ' +
  'usage - the user\'s or your own - that conflicts with an entry, propose a ' +
  'precise term when the language in play is fuzzy or overloaded, and treat ' +
  'a term resolved during this task as documentation impact to record in ' +
  'your report for the pending assessment, not a remark to lose. ' +
  'Documentation settling (before a change lands): the unit of ' +
  'documentation assessment ' +
  'is the change, not each task inside it. A task that modifies the system ' +
  'notes its documentation impact in its report and carries on - it does not ' +
  'summon the coordinator on its own. Before that change lands - that is, ' +
  'before you commit it or raise a pull request - read ' +
  'docs/_system/process/coordinator.md and run its bounded assessment over ' +
  'the accumulated change, which that file defines. Documentation must ship in the same change as the ' +
  'implementation it describes, so settling before the change lands is the ' +
  'only cadence that satisfies that rule. Landing a system-modifying change ' +
  'without settling is the failure this obligation exists to prevent - the ' +
  'assessment is cheap and most often concludes that no documentation ' +
  'change is justified, so run it rather than predicting its result.';

process.stdout.write(JSON.stringify({
  hookSpecificOutput: {
    hookEventName: 'UserPromptSubmit',
    additionalContext: context,
  },
}));
