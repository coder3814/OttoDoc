// Generated by OttoDoc. Do not edit - the next converge overwrites this file.
//
// Injects the documentation-routing obligation into every user prompt via the
// Claude Code UserPromptSubmit hook. The static CLAUDE.md block states the same
// obligation, but static context loses to task momentum on judgment tasks;
// context injected at prompt time does not. This script complements the block,
// it does not replace it.
'use strict';

const context =
  'Documentation routing (from the docs/ knowledge tree): Before forming any ' +
  'plan, evaluation, or recommendation - including judgments about ' +
  'issue-tracker items - open docs/index.md, descend the kind indexes whose ' +
  'reader-question bears on this task, and read the bearing documents. Code ' +
  'and tracker items answer only what exists; the tree answers what is ' +
  'intended, constrained, or already decided. A verdict reached before ' +
  'consulting the bearing documents is a guess, however well the code was ' +
  'read. Coverage is part of correctness: each index describes its documents ' +
  'in one sentence precisely so you can decide relevance without opening ' +
  'them - walk every kind index, list every document whose sentence bears on ' +
  'any part of the task, and read each one on that list before concluding. ' +
  'The common failure is reading two documents and stopping; the decisions ' +
  'index in particular tends to bear on far more of a backlog or evaluation ' +
  'task than it first appears.';

process.stdout.write(JSON.stringify({
  hookSpecificOutput: {
    hookEventName: 'UserPromptSubmit',
    additionalContext: context,
  },
}));
