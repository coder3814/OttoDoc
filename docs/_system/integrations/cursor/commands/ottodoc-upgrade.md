# OttoDoc upgrade

Replace the engine with the newest version and refresh every recorded platform.

This file is a generated Cursor adapter. Treat this invocation, together with the instructions that follow it, as the explicit command `OttoDoc upgrade`. Read `docs/_system/process/workflow.md` and `docs/_system/constitution.md` completely, then follow the canonical workflow for this action. If this adapter and the canonical engine disagree, the files under `docs/_system/` win.
